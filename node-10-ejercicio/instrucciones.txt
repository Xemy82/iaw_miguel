Crear un servidor que devuelva a una peticion a / el siguiente objeto en json
{
    lista: ["hola", "adios"]
}

Crear un cliente que haga una petición fetch al servidor


Y que la lista la añada al final de body como un ul

<ul>
    <li>hola</li>
    <li>adios</li>
</ul>